:8080 {
    # Serve static files from the Vite build output
    root * /srv
    encode gzip
    file_server

    # Handle SPA routing
    try_files {path} /index.html

    # Proxy /api requests to Gotenberg internally
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy localhost:3000
    }
}
